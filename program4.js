// JavaScript function to extract a specified number of characters from a string
truncate_string = (data, n) => {
  var subString1 = data.substr(0, n);
  console.log(subString1);
};

//twst cases
truncate_string("Robin Singh", 4);
